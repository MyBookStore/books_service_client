require 'spec_helper'

describe BooksServiceClient::Service do

  let(:service) {BooksServiceClient::Service.new}

  it "should give the url for books" do
    # expect(service.all_books).to eql('localhost:4444/index')
  end
end